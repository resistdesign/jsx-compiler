# JSX Compiler

Compile JSX using WebPack without a local WebPack config.

## Install

`npm i -g @resistdesign/jsx-compiler`

## Usage

1. `jsxc`
1. `jsxc src/app.jsx`
1. `jsxc src/**/*.jsx dist`

See: `jsxc -h`
